query GetCurrencies($after: String) {
    currencies(first: 10, after: $after) @paginate {
        edges {
            node {
                id
                ...CurrencyFieldFragment
            }
        }
        pageInfo {
            hasPreviousPage
            hasNextPage
        }
    }
}
